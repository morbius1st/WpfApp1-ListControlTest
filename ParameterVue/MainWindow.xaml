<Window Name="mainWindow" Title="MainWindow"
		x:Class="ParameterVue.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:cp="clr-namespace:ParameterVue.WpfSupport.CustomXAMLProperties"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:familyInfo="clr-namespace:ParameterVue.FamilyManager.FamilyInfo"
		xmlns:local="clr-namespace:ParameterVue"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:sys="clr-namespace:System;assembly=mscorlib"
		Width="600"
		Height="300"
		DataContext="{Binding ElementName=mainWindow}"
		Loaded="MainWindow_Loaded"
		mc:Ignorable="d"
		Background="#333333"
		>
	<Window.Resources>

		<Style x:Key="{x:Static local:MainWindow.ColHeaderTbkSTyleName}"
				TargetType="TextBlock"
				>
			<Setter Property="FontFamily" Value="{Binding ElementName=mainWindow, Path=lbc.HeaderFont.FontFamily, Mode=OneWay}" />
			<Setter Property="FontSize" Value="{Binding ElementName=mainWindow, Path=lbc.HeaderFont.FontSize, Mode=OneWay}" />
			<Setter Property="FontStretch" Value="{Binding ElementName=mainWindow, Path=lbc.HeaderFont.FontStretch, Mode=OneWay}" />
			<Setter Property="FontStyle" Value="{Binding ElementName=mainWindow, Path=lbc.HeaderFont.FontStyle, Mode=OneWay}" />
			<Setter Property="FontWeight" Value="{Binding ElementName=mainWindow, Path=lbc.HeaderFont.FontWeight, Mode=OneWay}" />
			<Setter Property="Foreground" Value="{Binding ElementName=mainWindow, Path=lbc.HeaderFont.Foreground, Mode=OneWay}" />
			<Setter Property="Padding" Value="2,4,2,4" />
			<Setter Property="TextWrapping" Value="Wrap" />
		</Style>

		<Style x:Key="ColHeaderSymb"
				TargetType="TextBlock"
				BasedOn="{StaticResource {x:Static local:MainWindow.ColHeaderTbkSTyleName}}"
				>
			<Setter Property="FontFamily" Value="{Binding ElementName=mainWindow, Path=lbc.SymbolFont.FontFamily, Mode=OneWay}" />
			<Setter Property="FontSize" Value="{Binding ElementName=mainWindow, Path=lbc.SymbolFont.FontSize, Mode=OneWay}" />
			<Setter Property="FontStretch" Value="{Binding ElementName=mainWindow, Path=lbc.SymbolFont.FontStretch, Mode=OneWay}" />
			<Setter Property="FontStyle" Value="{Binding ElementName=mainWindow, Path=lbc.SymbolFont.FontStyle, Mode=OneWay}" />
			<Setter Property="FontWeight" Value="{Binding ElementName=mainWindow, Path=lbc.SymbolFont.FontWeight, Mode=OneWay}" />
			<Setter Property="Foreground" Value="{Binding ElementName=mainWindow, Path=lbc.SymbolFont.Foreground, Mode=OneWay}" />
		</Style>


		<Style x:Key="FieldBase"
				TargetType="TextBox"
				>
			<Setter Property="VerticalContentAlignment" Value="Center" />
			<Style.Triggers>
				<Trigger Property="cp:CustomProperties.GenericBoolOne" Value="True">
					<Setter Property="Foreground" Value="Red" />
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="{x:Static local:MainWindow.RowHeaderTbxStyleName}"
				TargetType="TextBox"
				BasedOn="{StaticResource FieldBase}"
				>
			<Setter Property="FontFamily" Value="{Binding ElementName=mainWindow, Path=lbc.RowHeaderFont.FontFamily, Mode=OneWay}" />
			<Setter Property="FontSize" Value="{Binding ElementName=mainWindow, Path=lbc.RowHeaderFont.FontSize, Mode=OneWay}" />
			<Setter Property="FontStretch" Value="{Binding ElementName=mainWindow, Path=lbc.RowHeaderFont.FontStretch, Mode=OneWay}" />
			<Setter Property="FontStyle" Value="{Binding ElementName=mainWindow, Path=lbc.RowHeaderFont.FontStyle, Mode=OneWay}" />
			<Setter Property="FontWeight" Value="{Binding ElementName=mainWindow, Path=lbc.RowHeaderFont.FontWeight, Mode=OneWay}" />
			<Setter Property="Foreground" Value="{Binding ElementName=mainWindow, Path=lbc.RowHeaderFont.Foreground, Mode=OneWay}" />
		</Style>

		<Style x:Key="{x:Static local:MainWindow.FieldTbxStyleName}"
				TargetType="TextBox"
				BasedOn="{StaticResource FieldBase}"
				>
			<Setter Property="FontFamily" Value="{Binding ElementName=mainWindow, Path=lbc.ParameterFont.FontFamily, Mode=OneWay}" />
			<Setter Property="FontSize" Value="{Binding ElementName=mainWindow, Path=lbc.ParameterFont.FontSize, Mode=OneWay}" />
			<Setter Property="FontStretch" Value="{Binding ElementName=mainWindow, Path=lbc.ParameterFont.FontStretch, Mode=OneWay}" />
			<Setter Property="FontStyle" Value="{Binding ElementName=mainWindow, Path=lbc.ParameterFont.FontStyle, Mode=OneWay}" />
			<Setter Property="FontWeight" Value="{Binding ElementName=mainWindow, Path=lbc.ParameterFont.FontWeight, Mode=OneWay}" />
			<Setter Property="Foreground" Value="{Binding ElementName=mainWindow, Path=lbc.ParameterFont.Foreground, Mode=OneWay}" />

		</Style>

	</Window.Resources>

	<Grid
			Margin="0,0,-1.4,0.4"
			Width="595"
			HorizontalAlignment="Stretch"
			>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="105" />

		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="150" />
			<RowDefinition Height="150" />

		</Grid.RowDefinitions>

		<Button x:Name="buttonTest2" Content="Test 2"
				Grid.Row="0"
				Grid.Column="1"
				Margin="0,0,10.2,78"
				Width="84"
				Height="34"
				HorizontalAlignment="Right"
				VerticalAlignment="Bottom"
				RenderTransformOrigin="0.516,0.494"
				Click="Button_Test2"
				/>
		<Button x:Name="buttonTest1" Content="Test 1"
				Grid.Row="0"
				Grid.Column="1"
				Margin="0,0,10.2,39"
				Width="84"
				Height="34"
				HorizontalAlignment="Right"
				VerticalAlignment="Bottom"
				Click="Button_Test1"
				/>
		<Button x:Name="button" Content="Debug"
				Grid.Row="0"
				Grid.Column="1"
				Margin="0,0,10.2,0"
				Width="84"
				Height="34"
				HorizontalAlignment="Right"
				VerticalAlignment="Bottom"
				Click="btnDebug_Click"
				/>


		<ScrollViewer
				Grid.Row="0"
				Grid.Column="0"
				HorizontalScrollBarVisibility="Visible"
				VerticalScrollBarVisibility="Auto"
				>
			<!--  VerticalAlignment="Stretch"  -->
			<!--  HorizontalAlignment="Stretch"  -->
			<!--  VerticalAlignment="top"  -->
			<ListBox x:Name="listBox"
					HorizontalContentAlignment="Stretch"
					VerticalContentAlignment="Stretch"
					d:DataContext="{d:DesignInstance d:Type=local:MainWindow,
																					 IsDesignTimeCreatable=True}"
					Background="#444444"
					Foreground="White"
					ItemsSource="{Binding Fm.Fd}"
					>
				<ListBox.Template>
					<ControlTemplate>
						<DockPanel>
							<Border
									MaxHeight="30"
									DockPanel.Dock="Top"
									BorderThickness="2"
									BorderBrush="RoyalBlue"
									>
								<Grid Name="header"
										>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="20" />
										<ColumnDefinition Width="*" />
										<!-- <ColumnDefinition Width="*" /> -->
									</Grid.ColumnDefinitions>
									<TextBlock
											Grid.Column="0"
											Text="{Binding Fm.Cd.SelectedColumn, Mode=OneTime}"
											Style="{StaticResource ColHeaderSymb}"
											/>
									<TextBlock
											Grid.Column="1"
											Text="{Binding Fm.Cd.NameColumn, Mode=OneTime}"
											Style="{DynamicResource {x:Static local:MainWindow.ColHeaderTbkSTyleName}}"
											/>
									<!--  Style="{StaticResource ColHeaderTbk}"  -->

								</Grid>

							</Border>
							<ItemsPresenter Name="ItemsPresenter"
									Margin="0,0,0,0"
									DockPanel.Dock="Bottom"
									/>
						</DockPanel>
					</ControlTemplate>
				</ListBox.Template>
				<ListBox.ItemTemplate>
					<DataTemplate
							DataType="{x:Type familyInfo:FamilyData}"
							>

						<Grid Name="data"
								Margin="-4,0,0,0"
								>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="20" />
								<!-- <ColumnDefinition Width="100" /> -->
								<ColumnDefinition Width="*" />

								<!-- <ColumnDefinition Width="*" /> -->
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="20" />
							</Grid.RowDefinitions>
							<CheckBox
									Grid.Column="0"
									Margin="0,0,3,0"
									HorizontalAlignment="Center"
									VerticalAlignment="Center"
									IsChecked="{Binding Selected}"
									/>
							<TextBox
									Grid.Column="1"
									Text="{Binding Name}"
									Style="{DynamicResource {x:Static local:MainWindow.RowHeaderTbxStyleName}}"
									/>
							<!--  Style="{StaticResource RowHeaderTbx}"  -->
							<!--  <TextBox  -->
							<!--  Grid.Column="2"  -->
							<!--  cp:CustomProperties.GenericBoolOne="True"  -->
							<!--  Text="{Binding Name}"  -->
							<!--  Style="{DynamicResource {x:Static local:MainWindow.StyleName}}"  -->
							<!--  />  -->

						</Grid>

					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</ScrollViewer>

		<TextBlock Name="textBlock"
				Grid.Row="1"
				Width="0"
				Height="0"
				Text="1"
				/>

	</Grid>
</Window>
